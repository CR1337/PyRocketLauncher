
Usage: rl dns COMMAND

Manage the dns service.

Cmmands:

    install     Install the dns service
    uninstall   Uninstall the dns service
    wizard      Configure the dns service interactively
    status      Display wether the dns service is installed
    list        Display all dns entries
    help        Display this help

Examples:

    rl dns install

    rl dns status
